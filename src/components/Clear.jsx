import React, {
    Component,
    PropTypes,
} from 'react';

class Clear extends Component {
    render() {
        return (
            <div>Clear</div>
        );
    }
}

Clear.propTypes    = {};
Clear.defaultProps = {};

export default Clear;
